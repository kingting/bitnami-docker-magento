docker login --username=kingting
IMAGE=magento
TAG=$(grep BITNAMI_IMAGE_VERSION Dockerfile | awk -F= '{print $2}' | awk '{print $1}' | tr -d '"')
echo "Push kingting/${IMAGE}:${TAG} to docker.io"
docker push kingting/${IMAGE}:${TAG}
