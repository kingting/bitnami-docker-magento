IMAGE=magento
TAG=$(grep BITNAMI_IMAGE_VERSION Dockerfile | awk -F= '{print $2}' | awk '{print $1}' | tr -d '"')
echo $TAG
docker build -t kingting/${IMAGE}:$TAG .
echo "Date: `date` image: kingting/$IMAGE:$TAG created" >> .history
